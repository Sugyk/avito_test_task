package service

import "github.com/Sugyk/avito_test_task/internal/models"

func (s *Service) UsersSetIsActive(userID string, isActive bool) (models.User, error) {
	// TODO: implement the business logic to set user's active status
	return models.User{}, nil
}
